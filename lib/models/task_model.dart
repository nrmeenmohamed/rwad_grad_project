class TaskModel{
  String? title;
  String? description;
  int? categoryId;
  String? time;
  String? priority;
  int? done;

  TaskModel({
     this.title,
     this.description,
     this.categoryId,
     this.time,
     this.priority,
     this.done,
  });
}